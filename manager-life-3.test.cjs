'use strict';const assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm');global.window=global;global.state={calendar:{date:'2026-10-01'},training:{messages:[{title:'Scout klar',read:false}]},rolePromises:{items:[{deadline:'2026-10-03'}]}};global.managerRoster=()=>[{name:'A',fatigue:45},{name:'B',fatigue:10}];global.medicalReady=p=>p.name!=='B';global.pendingManagerDecision=()=>({title:'Kaptenen vill prata'});global.deskFixtures=()=>({upcoming:[{date:'2026-10-02',opponent:'Luleå',venue:'Hemma',type:'SHL'}]});global.calGap=(a,b)=>Math.round((new Date(b)-new Date(a))/86400000);global.trainingSafe=x=>x;global.deskNavigate=()=>{};vm.runInThisContext(fs.readFileSync('manager-life-3.js','utf8'));const rows=managerLifeAgenda();assert.equal(rows[0].urgency,'critical');assert.ok(rows.some(r=>r.kind==='conversation'));assert.ok(rows.some(r=>r.kind==='promise'));assert.ok(rows.some(r=>r.kind==='match'));assert.ok(rows.length<=5);console.log('PASS: manager agenda prioritizes real career state');